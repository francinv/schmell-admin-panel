import { Box } from "@mui/material";
import { styled } from "@mui/system";

export const Container95 = styled(Box)(({ theme }) => ({
    width: '95%',
    display:'flex',
    marginLeft: 'auto',
    marginRight: 'auto',
}));

